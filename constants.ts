import { Position } from './types';

export const GRID_SIZE = 10;
export const INITIAL_POINTS = 5;
export const TEAM_BLUE_START: Position = { row: 9, col: 0 };
export const TEAM_RED_START: Position = { row: 0, col: 9 };
